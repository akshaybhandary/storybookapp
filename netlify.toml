[build]
  command = "npm run build"
  publish = "dist"

[functions]
  # Increase timeout to maximum (26 seconds on Pro plan)
  node_bundler = "esbuild"
  
[functions.openrouter]
  # Maximum timeout - requires Pro plan
  timeout = 26
